#  Code generated by gocore. YOU ARE ALLOWED TO EDIT.
#  versions:
#   gocore v0.1.0

version: "2"
cloud:
  project: "blog"
sql:
  # Users data
  - engine: "postgresql"
    schema: "./internal/users/migration"
    queries: "./internal/users/query"

    gen:
      go:
        package: "users"
        out: "./internal/users/models"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
        emit_empty_slices: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: timestamptz
            go_type: time.Time

  # social auth
  - engine: "postgresql"
    schema: "./internal/socialauth/migration"
    queries: "./internal/socialauth/query"

    gen:
      go:
        package: "socialauth"
        out: "./internal/socialauth/models"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
        emit_empty_slices: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: timestamptz
            go_type: time.Time
